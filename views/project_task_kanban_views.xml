<?xml version="1.0" encoding="UTF-8"?>
<odoo>
  <record id="view_task_kanban_inherit_scrum" model="ir.ui.view">
    <field name="name">project.task.kanban.scrum</field>
    <field name="model">project.task</field>
    <!-- Ereditare la vista Kanban standard dei Task -->
    <field name="inherit_id" ref="project.view_task_kanban"/>
    <field name="arch" type="xml">
      <!-- Individuiamo il contenitore principale della card -->
      <xpath expr="//div[contains(@class,'o_project_kanban_main')]" position="inside">
        <!-- Aggiungiamo una nuova “box” accanto alle altre -->
        <div class="o_project_kanban_box me-1">
          <t t-if="record.project_type.raw_value === 'scrum'">
            <!-- Badge con i punti storia -->
            <span class="o_badge o_badge_info">
              <t t-esc="record.story_points.raw_value or 0"/>
            </span>
          </t>
        </div>
      </xpath>
    </field>
  </record>
</odoo>
